{% if dateNew %}
    {% set difference = date("today").diff(date(dateNew)) %}
    {% set leftDays = difference.days %}
    {% if leftDays <8 %}
        <span class="badge badge-primary">
            <i class="fas fa-certificate"></i> Nouveautés
        </span>
    {% endif %}
{% endif %}